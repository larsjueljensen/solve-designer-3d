--------------------------------------------------------------------------------
--  Function......... : AdjustComponentWidth
--  Author........... : 
--  Description...... : 
--------------------------------------------------------------------------------

--------------------------------------------------------------------------------
function ASPMain.AdjustComponentWidth ( hComponent, nSpaceWidth )
--------------------------------------------------------------------------------

-- 	local nViewportWidth = application.getCurrentUserViewportWidth ( )
--     local nCurrentWidth, nCurrentHeight = hud.getComponentSize ( hComponent )
--     local nWantedWidth = nViewportWidth - nSpaceWidth
--     
--     log.message ( "Viewport width = ", nViewportWidth, ". Current width = ", nCurrentWidth )
--     
--     if (nCurrentWidth ~= nWantedWidth) then
--         hud.setComponentSize ( hComponent, nSpaceWidth , nCurrentHeight )
--     end
--     
--     
    
--------------------------------------------------------------------------------
end
--------------------------------------------------------------------------------
